/**
 * utestCamera.cpp
 */

#include <cstdlib>
#include "PerspectiveCamera.h"

int main(int argc, char* argv[]) {
    Vector3D(0, 0, 0) - Vector3D(0, 0, 0);

    PerspectiveCamera camera("main", "perspective");

    exit(EXIT_SUCCESS);
}
